name: Test minimum coverage pass
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up mock lcov tools
        run: |
          mkdir -p /tmp/mock-bin

          # Mock genhtml: creates the output directory with a minimal HTML file
          printf '%s\n' '#!/bin/sh' \
            'outdir=""' \
            'prev=""' \
            'for arg in "$@"; do' \
            '  case "$prev" in' \
            '    --output-directory) outdir="$arg" ;;' \
            '  esac' \
            '  prev="$arg"' \
            'done' \
            'if [ -n "$outdir" ]; then' \
            '  mkdir -p "$outdir"' \
            '  printf "<html><body>Coverage</body></html>" > "$outdir/index.html"' \
            'fi' \
            'echo "genhtml: done"' \
            'exit 0' > /tmp/mock-bin/genhtml
          chmod +x /tmp/mock-bin/genhtml

          # Mock lcov: merges tracefiles by concatenation
          printf '%s\n' '#!/bin/sh' \
            'outfile=""' \
            'tracefiles=""' \
            'prev=""' \
            'for arg in "$@"; do' \
            '  case "$prev" in' \
            '    --output-file) outfile="$arg" ;;' \
            '    --add-tracefile) tracefiles="$tracefiles $arg" ;;' \
            '  esac' \
            '  prev="$arg"' \
            'done' \
            'if [ -n "$outfile" ]; then' \
            '  : > "$outfile"' \
            '  for tf in $tracefiles; do' \
            '    cat "$tf" >> "$outfile"' \
            '  done' \
            'fi' \
            'exit 0' > /tmp/mock-bin/lcov
          chmod +x /tmp/mock-bin/lcov

          echo "/tmp/mock-bin" >> "$GITHUB_PATH"

      - id: action
        uses: .
        with:
          coverage-files: tests/fixtures/lcov-full.info
          minimum-coverage: 90
          working-directory: tests/fixtures

      - name: Assert action succeeded and total-coverage meets minimum
        env:
          ACTUAL: ${{ steps.action.outputs.total-coverage }}
        run: |
          echo "actual total-coverage: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: total-coverage output is empty"
            exit 1
          fi
          # Coverage should be >= 90 (full coverage file has 100%)
          COV_INT=$(echo "$ACTUAL" | cut -d'.' -f1)
          if [ "$COV_INT" -lt 90 ]; then
            echo "FAIL: expected coverage >= 90, got $ACTUAL"
            exit 1
          fi
          echo "PASS: total-coverage=$ACTUAL meets minimum of 90"
